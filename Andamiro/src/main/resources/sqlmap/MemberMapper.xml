<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >

<mapper namespace="memberService" >

	<insert id="insertMember" parameterType="MemberDTO">
		INSERT INTO MEMBER(ID, PW, NAME, EMAIL, PHONE)
		SELECT #{id},#{pw},#{name},#{email},#{phone} FROM DUAL
		WHERE NOT EXISTS(SELECT * FROM MEMBER WHERE ID=#{id})
	</insert>
	
	<select id="selectMember" parameterType="LoginDTO" resultType="LoginDTO">
		SELECT ID, NAME
		FROM MEMBER
		WHERE ID=#{id} AND PW=#{pw}
	</select>

</mapper>
